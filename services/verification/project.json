{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "verification",
  "projectType": "application",
  "sourceRoot": "services/verification",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go build -o ../../bin/verification ./cmd/server",
        "cwd": "services/verification"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go test ./...",
        "cwd": "services/verification"
      }
    }
  }
}
